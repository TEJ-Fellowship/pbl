# Redis Performance Tuning Configuration

# Memory Management
maxmemory 256mb
maxmemory-policy allkeys-lru

# Persistence
appendonly yes
save 60 1000

# Network
tcp-keepalive 60

# Keyspace Notifications (for expired booking cleanup)
notify-keyspace-events Ex


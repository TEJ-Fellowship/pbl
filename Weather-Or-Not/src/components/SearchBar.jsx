const SearchBar = () => {
    return (
        <div>
            <input type="text" placeholder="Search for a city" />
            <button>Search</button>
        </div>
    )
}

export default SearchBar;